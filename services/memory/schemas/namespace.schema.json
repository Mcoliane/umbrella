{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "umbrella.memory.service.namespace.v1",
  "type": "object",
  "required": ["id", "owner_type", "owner_id", "visibility", "created_at", "updated_at"],
  "properties": {
    "id": {"type": "string"},
    "owner_type": {"type": "string", "enum": ["user", "team", "system"]},
    "owner_id": {"type": "string"},
    "visibility": {"type": "string", "enum": ["private", "shared", "public"]},
    "retention_days": {"type": ["integer", "null"]},
    "created_at": {"type": "string"},
    "updated_at": {"type": "string"}
  },
  "additionalProperties": true
}
